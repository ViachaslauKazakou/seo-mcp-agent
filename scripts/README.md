# Database Management Scripts

Набор bash-скриптов для управления PostgreSQL базой данных SEO MCP Agent.

## 📋 Список скриптов

### 1. `start-db.sh` - Запуск базы данных

Запускает PostgreSQL контейнер через Docker Compose и ждёт готовности.

```bash
./scripts/start-db.sh
```

**Что делает:**
- Проверяет, что Docker запущен
- Запускает контейнер `seo_agent_postgres`
- Ждёт готовности базы данных (healthcheck)
- Выводит информацию о подключении

**Вывод:**
```
🚀 Starting PostgreSQL database...
✅ PostgreSQL is up and running!

📊 Database connection info:
   Host: localhost
   Port: 5434
   Database: seo_agent
   User: seo_user
   Password: seo_password
```

---

### 2. `stop-db.sh` - Остановка базы данных

Останавливает PostgreSQL контейнер.

```bash
./scripts/stop-db.sh
```

**Примечание:** Данные сохраняются в Docker volume и не удаляются.

---

### 3. `migrate.sh` - Применение миграций

Применяет все pending миграции к базе данных.

```bash
./scripts/migrate.sh
```

**Что делает:**
- Проверяет подключение к БД
- Показывает текущую версию схемы
- Применяет миграции до последней версии
- Показывает итоговую версию

**Использование:**
```bash
# Применить все миграции
./scripts/migrate.sh

# С custom переменными окружения
POSTGRES_PORT=5435 ./scripts/migrate.sh
```

---

### 4. `create-migration.sh` - Создание новой миграции

Создаёт новую миграцию с автоматическим определением изменений схемы.

```bash
./scripts/create-migration.sh "Migration message"
```

**Примеры:**
```bash
# Создать миграцию для добавления таблицы
./scripts/create-migration.sh "Add user preferences table"

# Создать миграцию для изменения колонки
./scripts/create-migration.sh "Change keyword field length"
```

**Что делает:**
- Сравнивает текущую схему БД с моделями SQLAlchemy
- Генерирует миграцию с изменениями
- Сохраняет в `src/db/migrations/versions/`

**После создания:**
1. Проверьте файл миграции
2. Примените через `./scripts/migrate.sh`

---

### 5. `init-db.sh` - Инициализация базы данных

Создаёт таблицы напрямую без миграций (для разработки).

```bash
./scripts/init-db.sh
```

**Использовать когда:**
- Первый запуск проекта
- Чистая база данных
- Альтернатива миграциям для dev

---

### 6. `reset-db.sh` - Полный сброс базы данных

**⚠️ ОПАСНО:** Удаляет все данные и пересоздаёт схему.

```bash
./scripts/reset-db.sh
```

**Что делает:**
- Запрашивает подтверждение
- Откатывает все миграции (downgrade base)
- Применяет все миграции заново (upgrade head)

**Использовать когда:**
- Нужно очистить тестовые данные
- Проблемы с миграциями
- Сброс к начальному состоянию

---

### 7. `db-shell.sh` - PostgreSQL CLI

Подключается к PostgreSQL через psql.

```bash
./scripts/db-shell.sh
```

**Примеры команд в psql:**
```sql
-- Список таблиц
\dt

-- Структура таблицы
\d websites

-- Запрос данных
SELECT * FROM websites;

-- Выход
\q
```

---

### 8. `db-logs.sh` - Просмотр логов

Показывает логи PostgreSQL в реальном времени.

```bash
./scripts/db-logs.sh
```

**Выход:** `Ctrl+C`

---

## 🚀 Быстрый старт

### Первый запуск проекта

```bash
# 1. Запустить базу данных
./scripts/start-db.sh

# 2. Применить миграции
./scripts/migrate.sh

# Готово! База данных настроена
```

### Ежедневная работа

```bash
# Запустить БД утром
./scripts/start-db.sh

# Работать...

# Остановить БД вечером
./scripts/stop-db.sh
```

### Изменение схемы

```bash
# 1. Изменить models.py
vim src/db/models.py

# 2. Создать миграцию
./scripts/create-migration.sh "Add new field to website"

# 3. Проверить миграцию
cat src/db/migrations/versions/*.py

# 4. Применить миграцию
./scripts/migrate.sh
```

---

## 🔧 Переменные окружения

Все скрипты используют следующие переменные (с дефолтными значениями):

```bash
POSTGRES_HOST=localhost      # Хост БД
POSTGRES_PORT=5434          # Порт БД
POSTGRES_DB=seo_agent       # Имя БД
POSTGRES_USER=seo_user      # Пользователь
POSTGRES_PASSWORD=seo_password  # Пароль
```

### Переопределение:

```bash
# Для одной команды
POSTGRES_PORT=5435 ./scripts/migrate.sh

# Экспорт для сессии
export POSTGRES_PORT=5435
./scripts/migrate.sh
```

---

## 📊 Alembic команды напрямую

Если нужен больший контроль:

```bash
cd src/db

# Текущая версия
poetry run alembic current

# История миграций
poetry run alembic history

# Применить следующую миграцию
poetry run alembic upgrade +1

# Откатить последнюю миграцию
poetry run alembic downgrade -1

# Откатить к конкретной ревизии
poetry run alembic downgrade <revision_id>
```

---

## 🐳 Docker Compose команды

```bash
# Запустить все сервисы
docker-compose up -d

# Запустить с PGAdmin
docker-compose --profile admin up -d

# Остановить все
docker-compose down

# Удалить данные
docker-compose down -v

# Просмотр статуса
docker-compose ps

# Логи
docker-compose logs -f postgres
```

---

## 📁 Структура файлов

```
scripts/
├── start-db.sh           # Запуск БД
├── stop-db.sh            # Остановка БД
├── migrate.sh            # Применение миграций
├── create-migration.sh   # Создание миграции
├── init-db.sh           # Инициализация БД
├── reset-db.sh          # Сброс БД
├── db-shell.sh          # PostgreSQL CLI
└── db-logs.sh           # Просмотр логов

src/db/
├── models.py            # SQLAlchemy модели
├── manager.py           # Database Manager
├── init_db.py          # Скрипт инициализации
├── alembic.ini         # Alembic конфигурация
└── migrations/         # Миграции Alembic
    ├── env.py
    └── versions/       # Файлы миграций
```

---

## 🔍 Troubleshooting

### База данных не запускается

```bash
# Проверить Docker
docker info

# Проверить логи
./scripts/db-logs.sh

# Пересоздать контейнер
docker-compose down
docker-compose up -d postgres
```

### Ошибка подключения к БД

```bash
# Проверить, что контейнер запущен
docker-compose ps

# Проверить порт
netstat -an | grep 5434

# Проверить переменные окружения
echo $POSTGRES_HOST
```

### Проблемы с миграциями

```bash
# Проверить текущую версию
cd src/db
poetry run alembic current

# Посмотреть историю
poetry run alembic history

# Откатить и применить заново
poetry run alembic downgrade -1
poetry run alembic upgrade +1
```

---

## 💡 Best Practices

1. **Всегда проверяйте миграции** перед применением
2. **Делайте backup** перед `reset-db.sh`
3. **Используйте осмысленные названия** для миграций
4. **Тестируйте миграции** на dev окружении
5. **Коммитьте файлы миграций** в git

---

## 📚 Дополнительная информация

- [Alembic Documentation](https://alembic.sqlalchemy.org/)
- [SQLAlchemy ORM](https://docs.sqlalchemy.org/en/20/orm/)
- [PostgreSQL Docker Image](https://hub.docker.com/_/postgres)
- [pgvector Extension](https://github.com/pgvector/pgvector)
